components:
  schemas:
    PageSort:
      properties:
        page:
          default: 1
          minimum: 1
          type: integer
        perPage:
          default: 25
          type: integer
        field:
          default: id
          enum:
            - id
          type: string
        order:
          default: ASC
          enum:
            - ASC
            - DESC
          type: string
      type: object
info:
  description: API
  title: API
  version: 1.0.0
openapi: 3.0.0
paths:
  /deep_object:
    get:
      operationId: getDeepObject
      parameters:
        - explode: true
          in: query
          name: pagesort
          schema:
            $ref: "#/components/schemas/PageSort"
          style: deepObject
      responses:
        "200":
          description: description
          content:
            application/json:
              schema:
                items:
                  type: number
                type: array
